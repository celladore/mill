# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Local agent/tool worktrees and leftover nested checkout (not part of the package)
.codex/
.serena/
/xtox/

# IDE and editors
.idea/
.vscode/
*.swp
*.swo
*~

# Dependencies
node_modules/
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Testing
/coverage
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/

# Next.js
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# Production builds
/build
frontend/build/
dist/
dist
develop-eggs/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/

# Allow lib/transcription but ignore its build artifacts
!lib/transcription/
lib/transcription/node_modules/
lib/transcription/dist/
lib/transcription/package-lock.json
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Environment files (comprehensive coverage)
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/
*token.json*
*credentials.json*

# The venv patterns above (env/, ENV/) match any directory literally named
# "env" at any depth, including infra/env/ — Terraform's own environment
# split, unrelated to Python virtualenvs. Un-ignore it explicitly; a
# negation after an exclusion only works if the directory itself is
# un-ignored first, so both lines are required.
!infra/env/
!infra/env/**

# Terraform (infra/env/*, infra/modules/*). Placed after the !infra/env/**
# negation above on purpose — exclude patterns after a negation still apply
# on top of it, so local state/crash artifacts stay ignored even though the
# directory itself is un-ignored.
**/.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
*.tfvars.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
# Lock file IS tracked (pins provider versions) — do not add .terraform.lock.hcl here.

# Logs and debug files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
dump.rdb
*.log
pip-log.txt
pip-delete-this-directory.txt

# System files
.DS_Store
Thumbs.db
*.pem

# Python
__pycache__/
*.py[cod]
*$py.class
*.so

# Distribution / packaging
.Python

# PyInstaller
*.manifest
*.spec

# Translations
*.mo
*.pot

# Django stuff:
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
.python-version

# pipenv
Pipfile.lock

# PEP 582
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# Azure Functions
local.settings.json
.azure/

# LaTeX auxiliary files
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.out
*.synctex.gz
*.toc
*.nav
*.snm
*.vrb

# Development tools
chainlit.md
.chainlit
.ac

# Deployment
.vercel

# Data and databases
agenthub/agents/youtube/db

# Archive files and large assets
**/*.zip
**/*.tar.gz
**/*.tar
**/*.tgz
*.pack
*.deb
*.dylib

# Build caches
.cache/

# Mobile development
android-sdk/
